<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Màu nền -->
    <solid android:color="#2CCE57" />
    <!-- Độ bo góc (radius) -->
    <corners android:radius="98dp" />
    <!-- Viền (nếu cần) -->
    <stroke
        android:width="1dp"
        android:color="#2CCE57" />
</shape>